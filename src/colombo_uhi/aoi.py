"""Study-area geometries: CMC core, Colombo District, rural references (Phase 1 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * Colombo Municipal Council (~37 km2) urban-core geometry
    * Colombo District boundary from FAO GAUL level-2 (ADM2_NAME == 'Colombo')
    * GN-division FeatureCollection loaded from the user-uploaded EE asset at
      params ``aoi.assets.gn_divisions`` — with a clear, actionable error
      while that asset id is null
    * rural-reference geometries (buffer ring AND LCZ-based) for SUHII
    * water mask from JRC GSW occurrence + named water bodies
"""
