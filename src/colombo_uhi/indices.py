"""Spectral indices: NDVI, NDBI, MNDWI, EVI, SAVI, albedo (Phase 2 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * index computation on scaled C2 L2 surface reflectance, using the
      per-sensor band mappings in params ``datasets.*.sr_bands`` (TM/ETM+ and
      OLI band numbers differ)
    * NDVI, NDBI, MNDWI, EVI, SAVI and shortwave albedo helpers, each taking
      and returning typed ``ee.Image`` objects
"""
