"""Scenario projection: RF regression + CA-Markov glue (Phase 6 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * Random Forest regression of LST on drivers (land cover, built-up
      surface, NDVI/NDBI, elevation, population, night lights)
    * CA-Markov land-cover transition glue for future scenarios
    * validation metrics shipped with every output: RMSE, R2, Kappa
    * framing enforced everywhere: conditional SCENARIO PROJECTION, never a
      forecast (non-negotiable caveat #3)
"""
