"""Visualisation: geemap map builders + matplotlib figure helpers (Phase 8 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * geemap interactive map builders for LST, UTFVI, trend and priority layers
    * matplotlib report-figure helpers with consistent styling
    * every figure stamped with the applicable caveat strings from params
      ``caveats`` (LST != air temperature; valid-observation counts; scenario
      != forecast)
"""
