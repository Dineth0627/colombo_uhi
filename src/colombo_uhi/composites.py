"""Seasonal/annual compositing with valid-observation counts (Phase 2 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * annual and seasonal (SW monsoon, NE monsoon, Jan-Mar dry window)
      composites of masked LST and indices
    * a per-pixel valid-observation count band emitted alongside EVERY
      composite (non-negotiable caveat #2 — tropical cloud cover)
    * median/mean reducer choices recorded in outputs' metadata
"""
