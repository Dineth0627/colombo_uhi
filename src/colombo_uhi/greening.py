"""Urban greening priority: MCDA / AHP weighted overlay (Phase 7 — NOT YET WRITTEN).

STUB — docstring only; no implementation exists yet. Do not import expecting
functionality.

Planned contents (per CLAUDE.md):
    * multi-criteria decision analysis (weighted overlay) for greening
      priority, criteria and weights from params ``greening``
    * AHP pairwise-comparison weighting with consistency-ratio check
      (CR <= params ``greening.ahp.consistency_ratio_max``) — pure-Python,
      unit-tested with synthetic matrices
"""
